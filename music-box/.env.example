# 复制成 .env 或直接在 ecosystem.config.cjs 里改。本地 dev 用 .env。
PORT=8080
NCM_BASE=http://127.0.0.1:3000
COOKIE_FILE=./data/ncm_cookie.txt
STATE_FILE=./data/state.json
# 点歌横幅署名
PICK_FROM=幸村
# 给 /mcp 加锁·编一串长的·Claude 连接器里填同一串
MCP_AUTH_TOKEN=
